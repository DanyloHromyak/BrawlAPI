import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: '#2979ff',
      light: '#448aff',
      dark: '#2962ff',
    },
  },
});

export default theme;